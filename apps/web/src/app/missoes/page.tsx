import { activeMissions } from "../../lib/engagement/queries"; import { completeMission } from "../../lib/engagement/actions"; export default async function Page(){ const missions=await activeMissions(); return <main><h1>Missões diárias</h1><p>Regressa todos os dias, sem deixar que as missões substituam o mérito das previsões.</p><div className="grid">{missions.map(m=><article className="card" key={m.id}><h2>{m.title}</h2><p>{m.description}</p><strong>{m.points} pontos</strong><form action={completeMission}><input type="hidden" name="missionId" value={m.id}/><button>Concluir missão</button></form></article>)}</div></main>}
